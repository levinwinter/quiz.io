<?xml version="1.0" encoding="UTF-8"?>
<project version="4">
  <component name="JavaScriptLibraryMappings">
    <file url="file://$PROJECT_DIR$" libraries="{@types/express, @types/socket.io, all, jquery}" />
    <file url="file://$PROJECT_DIR$/public/js/user.js" libraries="{@types/express, @types/socket.io, all, jquery}" />
    <file url="PROJECT" libraries="{@types/express, @types/jquery, @types/socket.io, all, jquery}" />
    <includedPredefinedLibrary name="Node.js Core" />
    <excludedPredefinedLibrary name="HTML" />
    <excludedPredefinedLibrary name="quiz.io/node_modules" />
  </component>
</project>